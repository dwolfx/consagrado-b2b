{
    table.status === 'free' && (
        <span style={{ fontSize: '0.8rem', marginTop: '0.5rem', opacity: 0.6 }}>Livre</span>
    )
}
                    </div >
                ))}
            </div >
        </div >
    );
};

export default Dashboard;
